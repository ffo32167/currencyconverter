-- восстановить main

-+ доделать cron(пока отложим)
-- zap внедряю

-- прокинуть ctx

-+ книга DDD Эрик Эванс(про единый язык)

-- реализовать два способа запуска синхронизации(cron и какой-то другой отличный) Sleep, для создания interface

-- добавить кеширование PG(сохраняем в памяти все данные полученные из PG и при повторном обращении используем их)

-- прочитать GRPC Uber/prototool(ознакомится)

-- запилить обработку ошибки в кроне при запуске синхронизации
вопросы:
    где создавать контекст(изучать на примере http)
    методы с фиктивными параметрами

-- в домене, для функций relation и rates создать структуру CurrencyRepository и привязать к ней эти методы.
    для дальнейшего расширения

-- переделать функцию toDomain как метод для типа CbrResponse

-- поднять redis в docker
-- поднять redis в docker Compose
-- поднять PG в docker
-- поднять PG в docker Compose
-- докеризировать CurrencyConverter