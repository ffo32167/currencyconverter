-- доделать cron(пока отложим)
-- zap внедряю

++ добавить реализацию методов postgres в интерфейс storage
++ пока не усложняем код(storage это скрипач)
    storage интерфейс я делаю, но не делаю фабрику стораджей
    общаться с постгресом только через интерфейс(писать бизнес логику в currency)

++ прокинуть ctx
++ вынести обмен из relation handler в домен, избавиться от postgres в relation handler
-- книга DDD Эрик Эванс(про единый язык)

++ создать источник валют

-- проверить курсы Currencyfreaks и Cbr

-- в домене описать процесс синхронизации(поход в источник и запись в хранилище)

-- реализовать два способа запуска синхронизации(cron и какой-то другой отличный)

-- добавить redis(storage)
++ добавить источник(аналог Currencyfreaks)
-- добавить кеширование PG(сохраняем в памяти все данные полученные из PG и при повторном обращении используем их)


-- прочитать GRPC Uber/prototool(ознакомится)
